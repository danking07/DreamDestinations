/* Style the whole page */
body {
  font-family: Georgia, serif;
  background-color: #fdf6e3;
  color: #3b2a00;
  max-width: 500px;        /* Keep everything centered and not too wide */
  margin: 40px auto;       /* Center the page horizontally */
  padding: 20px;
}

h1 {
  font-size: 2rem;
  color: #7a4e00;
}

/* The form box */
.form {
  background: white;
  border: 2px solid #e0c080;
  border-radius: 12px;
  padding: 24px;
  margin-top: 20px;
}

/* Put each label and input on its own line with spacing */
label {
  display: block;
  margin-top: 14px;
  font-weight: bold;
  font-size: 0.9rem;
}

input, select {
  display: block;
  width: 100%;
  padding: 10px;
  margin-top: 6px;
  border: 1.5px solid #ccc;
  border-radius: 8px;
  font-size: 1rem;
}

/* Style the button */
button {
  margin-top: 20px;
  width: 100%;
  padding: 12px;
  background-color: #7a4e00;
  color: white;
  font-size: 1rem;
  border: none;
  border-radius: 8px;
  cursor: pointer;
}

button:hover {
  background-color: #5c3900;   /* Darker when mouse hovers over it */
}

/* The result box */
.result {
  margin-top: 24px;
  background: #7a4e00;
  color: white;
  text-align: center;
  padding: 28px;
  border-radius: 12px;
  font-size: 1.2rem;
}

.result span {
  display: block;
  font-size: 2.5rem;
  font-weight: bold;
  margin: 10px 0;
}

/* This class hides an element completely */
.hidden {
  display: none;
}